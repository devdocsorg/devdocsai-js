---
'@devdocsai/docusaurus-theme-search': major
---

**BREAKING**: Migrate to Docusaurus 3. The `@docusaurus/types` dep is now `^3.10.0` (was `^2.4.1`) and the peer `react` is now `^18 || ^19` (was `^17.0.0`).

Migration: upgrade your Docusaurus site to 3.x first, then bump `@devdocsai/docusaurus-theme-search`. If you must stay on Docusaurus 2, pin to `@devdocsai/docusaurus-theme-search@^0.14.x`.
